  name: Test and Lint

  on:
    push:
      branches:
        - main
    pull_request:

  jobs:
    container:
      runs-on: ubuntu-24.04

      steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run pylint
        run: pylint main.py src
